# 发现源实现指导 v0.8.0

## 一、发现源严格推理分析

### 1.1 发现源分类与特性

| 发现源 | 协议类型 | 发现范围 | 实时性 | 离线支持 | 权限控制 | 适用场景 |
|--------|----------|----------|--------|----------|----------|----------|
| UDP Broadcast | 无连接广播 | 局域网 | 高 | 否 | 无 | 个人网络、部门分享 |
| mDNS/DNS-SD | 服务发现 | 局域网 | 高 | 否 | 无 | 个人网络 |
| DHT (Kademlia) | 分布式哈希 | 全球 | 中 | 否 | 可选 | 部门分享、公司管理、公共社区 |
| SkillCenter API | RESTful API | 全球 | 中 | 否 | 强 | 公司管理、公共社区 |
| GitHub/Gitee | Git协议 | 全球 | 低 | 是 | 强 | 公共社区 |
| Git Repository | Git协议 | 企业内 | 低 | 是 | 中 | 公司管理 |
| Local FS | 文件系统 | 本地 | 无 | 是 | 系统 | 所有场景（缓存） |

### 1.2 发现源能力矩阵

| 发现源 | 场景发现 | 能力发现 | 技能发现 | Agent发现 | 实时推送 |
|--------|----------|----------|----------|-----------|----------|
| UDP Broadcast | ✅ | ✅ | ✅ | ✅ | ✅ |
| mDNS/DNS-SD | ✅ | ✅ | ✅ | ✅ | ❌ |
| DHT | ✅ | ✅ | ✅ | ❌ | ❌ |
| SkillCenter API | ✅ | ✅ | ✅ | ❌ | ✅(WebSocket) |
| GitHub/Gitee | ✅ | ✅ | ✅ | ❌ | ❌ |
| Git Repository | ✅ | ✅ | ✅ | ❌ | ❌ |
| Local FS | ✅ | ✅ | ✅ | ❌ | ❌ |

---

## 二、发现流程与发现源映射

### 2.1 发现流程阶段分析

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    发现流程阶段与发现源映射                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  阶段1: 启动雷达扫描                                                         │
│  ─────────────────────────────────────────────────────────────              │
│  输入: 用户触发/自动触发                                                     │
│  输出: 索引数据（scene-index.yaml, cap-index.yaml, skill-index.yaml）       │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 发现源选择策略                                                        │   │
│  │                                                                       │   │
│  │ 个人网络: Local FS(缓存) → UDP Broadcast → mDNS                      │   │
│  │ 部门分享: Local FS(缓存) → UDP Broadcast → DHT                       │   │
│  │ 公司管理: Local FS(缓存) → SkillCenter API → Git Repository          │   │
│  │ 公共社区: Local FS(缓存) → SkillCenter API → GitHub → DHT            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  阶段2: 展示选择界面                                                         │
│  ─────────────────────────────────────────────────────────────              │
│  输入: 索引数据                                                             │
│  输出: 分类展示数据                                                         │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 数据聚合策略                                                          │   │
│  │                                                                       │   │
│  │ 1. 合并多个发现源的结果（去重）                                        │   │
│  │ 2. 标注数据来源（提供者名称）                                          │   │
│  │ 3. 标注数据新鲜度（时间戳）                                            │   │
│  │ 4. 标注数据可信度（官方/社区/个人）                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  阶段3: 用户选择场景                                                         │
│  ─────────────────────────────────────────────────────────────              │
│  输入: 用户选择                                                             │
│  输出: 场景详情、可用技能列表                                               │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 详情获取策略                                                          │   │
│  │                                                                       │   │
│  │ 1. 优先从 Local FS 获取缓存详情                                       │   │
│  │ 2. 从原始发现源获取最新详情                                            │   │
│  │ 3. 聚合多个发现源的技能列表                                            │   │
│  │ 4. 展示技能对比信息（性能、评分、下载量）                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  阶段4: 自动安装场景                                                         │
│  ─────────────────────────────────────────────────────────────              │
│  输入: 用户确认                                                             │
│  输出: 安装结果                                                             │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 安装策略                                                              │   │
│  │                                                                       │   │
│  │ 1. 从发现源下载技能包                                                  │   │
│  │ 2. 验证 CAP 契约                                                      │   │
│  │ 3. 安装依赖技能                                                       │   │
│  │ 4. 初始化场景 Agent                                                   │   │
│  │ 5. 激活场景                                                           │   │
│  │ 6. 缓存到 Local FS                                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 发现源优先级策略

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    发现源优先级策略                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  策略1: 速度优先（个人网络）                                                 │
│  ─────────────────────────────────────────────────────────────              │
│  Local FS (0ms) → UDP Broadcast (10-50ms) → mDNS (50-100ms)                │
│                                                                             │
│  策略2: 覆盖优先（部门分享）                                                 │
│  ─────────────────────────────────────────────────────────────              │
│  Local FS (缓存) → UDP Broadcast (局域网) → DHT (跨网段)                   │
│                                                                             │
│  策略3: 可信度优先（公司管理）                                               │
│  ─────────────────────────────────────────────────────────────              │
│  Local FS (缓存) → SkillCenter API (官方) → Git Repository (企业内)        │
│                                                                             │
│  策略4: 丰富度优先（公共社区）                                               │
│  ─────────────────────────────────────────────────────────────              │
│  Local FS (缓存) → SkillCenter API (官方) → GitHub (社区) → DHT (分布式)   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、各发现源实现指导

### 3.1 UDP Broadcast 发现源

#### 3.1.1 协议实现

```java
public class UdpBroadcastDiscoveryProvider implements DiscoveryProvider {
    
    // UDP 广播配置
    private static final int BROADCAST_PORT = 48888;
    private static final String BROADCAST_ADDRESS = "255.255.255.255";
    private static final int BUFFER_SIZE = 4096;
    private static final int ANNOUNCE_INTERVAL = 5000; // 5秒
    
    // 消息类型
    private static final byte MSG_AGENT_ANNOUNCE = 0x01;
    private static final byte MSG_CAP_SHARE = 0x02;
    private static final byte MSG_SCENE_CREATE = 0x03;
    private static final byte MSG_CAP_HANDOVER = 0x04;
    private static final byte MSG_CAP_SYNC = 0x05;
    
    // 发现流程支持
    @Override
    public CompletableFuture<List<DiscoveredItem>> discover(DiscoveryQuery query) {
        return CompletableFuture.supplyAsync(() -> {
            List<DiscoveredItem> results = new ArrayList<>();
            
            // 1. 发送发现请求广播
            sendDiscoveryRequest(query);
            
            // 2. 等待响应（超时5秒）
            long startTime = System.currentTimeMillis();
            while (System.currentTimeMillis() - startTime < 5000) {
                List<DiscoveredItem> items = receiveDiscoveryResponses();
                results.addAll(items);
            }
            
            return results;
        });
    }
    
    // 消息格式
    // AGENT_ANNOUNCE:{agentId};{userId};{deviceName};{capabilities};{scenes};{status};{timestamp}
    private void sendDiscoveryRequest(DiscoveryQuery query) {
        String message = buildDiscoveryMessage(query);
        byte[] data = message.getBytes();
        DatagramPacket packet = new DatagramPacket(
            data, data.length,
            InetAddress.getByName(BROADCAST_ADDRESS),
            BROADCAST_PORT
        );
        socket.send(packet);
    }
}
```

#### 3.1.2 发现流程支持

| 流程阶段 | 支持方式 | 实现要点 |
|----------|----------|----------|
| 启动雷达扫描 | 广播发现请求 | 发送 DISCOVERY_REQUEST 消息，等待响应 |
| 展示选择界面 | 接收广播响应 | 解析 AGENT_ANNOUNCE、CAP_SHARE、SCENE_CREATE 消息 |
| 用户选择场景 | 实时更新 | 持续监听广播，更新可用场景列表 |
| 自动安装场景 | 不支持 | 需要其他发现源支持下载 |

#### 3.1.3 可视化指导

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  UDP Broadcast 发现可视化                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  界面元素:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 雷达扫描动画（表示正在扫描局域网）                                       │
│  2. 设备列表（实时更新）                                                    │
│     ├── 设备图标（在线🟢/离线🔴/休眠🔵）                                    │
│     ├── 设备名称                                                            │
│     ├── 可用能力数量                                                        │
│     └── 可用场景数量                                                        │
│  3. 能力列表（聚合所有设备）                                                │
│  4. 场景列表（聚合所有设备）                                                │
│                                                                             │
│  交互流程:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 点击"扫描"按钮 → 显示雷达动画                                           │
│  2. 发现新设备 → 设备列表实时更新                                            │
│  3. 点击设备 → 展示设备详情和能力列表                                        │
│  4. 点击能力 → 展示能力详情和可用技能                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 3.2 mDNS/DNS-SD 发现源

#### 3.2.1 协议实现

```java
public class MdnsDiscoveryProvider implements DiscoveryProvider {
    
    // mDNS 服务类型
    private static final String SERVICE_TYPE = "_ooder._tcp.local.";
    
    // 服务注册信息
    private static final String SERVICE_NAME = "Ooder Agent";
    private static final int SERVICE_PORT = 48889;
    
    // 发现流程支持
    @Override
    public CompletableFuture<List<DiscoveredItem>> discover(DiscoveryQuery query) {
        return CompletableFuture.supplyAsync(() -> {
            List<DiscoveredItem> results = new ArrayList<>();
            
            // 1. 查询 mDNS 服务
            ServiceInfo[] services = jmdns.list(SERVICE_TYPE);
            
            // 2. 解析服务信息
            for (ServiceInfo service : services) {
                DiscoveredItem item = parseServiceInfo(service, query.getType());
                if (item != null) {
                    results.add(item);
                }
            }
            
            return results;
        });
    }
    
    // 服务注册
    public void registerService(String agentId, String[] capabilities, String[] scenes) {
        Map<String, String> props = new HashMap<>();
        props.put("agentId", agentId);
        props.put("capabilities", String.join(",", capabilities));
        props.put("scenes", String.join(",", scenes));
        
        ServiceInfo serviceInfo = ServiceInfo.create(
            SERVICE_TYPE,
            SERVICE_NAME + "-" + agentId,
            SERVICE_PORT,
            0, 0,
            props
        );
        
        jmdns.registerService(serviceInfo);
    }
}
```

#### 3.2.2 发现流程支持

| 流程阶段 | 支持方式 | 实现要点 |
|----------|----------|----------|
| 启动雷达扫描 | 查询 mDNS 服务 | 使用 JmDNS 查询 _ooder._tcp.local. 服务 |
| 展示选择界面 | 解析服务属性 | 从 TXT 记录中提取 capabilities、scenes 信息 |
| 用户选择场景 | 服务详情查询 | 通过服务端口获取详细信息 |
| 自动安装场景 | 不支持 | 需要其他发现源支持下载 |

#### 3.2.3 可视化指导

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  mDNS 发现可视化                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  界面元素:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 服务发现列表                                                            │
│     ├── 服务名称                                                            │
│     ├── 服务类型（Agent/Scene/Skill）                                       │
│     ├── 服务地址和端口                                                      │
│     └── 服务属性（capabilities、scenes）                                    │
│  2. 服务详情面板                                                            │
│     ├── 基本信息                                                            │
│     ├── 能力列表                                                            │
│     └── 场景列表                                                            │
│                                                                             │
│  交互流程:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 自动发现 → 服务列表自动更新                                              │
│  2. 点击服务 → 展示服务详情                                                  │
│  3. 点击能力 → 展示能力详情                                                  │
│  4. 点击场景 → 展示场景详情                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 3.3 DHT (Kademlia) 发现源

#### 3.3.1 协议实现

```java
public class DhtDiscoveryProvider implements DiscoveryProvider {
    
    // DHT 节点配置
    private static final int K = 20; // k-bucket 大小
    private static final int ALPHA = 3; // 并发查询数
    
    // 键空间设计
    // scene:{sceneId} -> SceneInfo
    // cap:{capId} -> CapabilityInfo
    // skill:{skillId} -> SkillInfo
    // agent:{agentId} -> AgentInfo
    
    // 发现流程支持
    @Override
    public CompletableFuture<List<DiscoveredItem>> discover(DiscoveryQuery query) {
        return CompletableFuture.supplyAsync(() -> {
            List<DiscoveredItem> results = new ArrayList<>();
            
            // 1. 构建查询键
            String key = buildQueryKey(query);
            
            // 2. DHT 查找
            List<Node> nodes = dht.findNode(key);
            
            // 3. 获取值
            for (Node node : nodes) {
                byte[] value = dht.get(key, node);
                if (value != null) {
                    DiscoveredItem item = parseValue(value, query.getType());
                    results.add(item);
                }
            }
            
            return results;
        });
    }
    
    // 发布到 DHT
    public void publish(String type, String id, byte[] data) {
        String key = type + ":" + id;
        dht.put(key, data);
    }
}
```

#### 3.3.2 发现流程支持

| 流程阶段 | 支持方式 | 实现要点 |
|----------|----------|----------|
| 启动雷达扫描 | DHT 查询 | 使用 Kademlia 协议查询键空间 |
| 展示选择界面 | 键空间遍历 | 遍历 scene:*, cap:*, skill:* 键前缀 |
| 用户选择场景 | 键值获取 | 通过 scene:{sceneId} 获取详细信息 |
| 自动安装场景 | P2P 下载 | 从 DHT 节点下载技能包 |

#### 3.3.3 可视化指导

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  DHT 发现可视化                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  界面元素:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. DHT 网络拓扑图                                                          │
│     ├── 节点分布（环形显示）                                                │
│     ├── 当前节点位置                                                        │
│     └── 查询路径动画                                                        │
│  2. 查询进度指示                                                            │
│     ├── 已查询节点数                                                        │
│     ├── 响应节点数                                                          │
│     └── 查询耗时                                                            │
│  3. 结果列表                                                                │
│     ├── 数据来源节点                                                        │
│     ├── 数据新鲜度                                                          │
│     └── 数据大小                                                            │
│                                                                             │
│  交互流程:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 输入查询关键词 → 显示查询路径动画                                        │
│  2. 查询完成 → 展示结果列表                                                  │
│  3. 点击结果 → 展示详情                                                      │
│  4. 点击下载 → 从 DHT 节点下载                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 3.4 SkillCenter API 发现源

#### 3.4.1 协议实现

```java
public class SkillCenterDiscoveryProvider implements DiscoveryProvider {
    
    // API 端点
    private static final String API_BASE_URL = "https://skillcenter.ooder.io/api/v1";
    
    // 发现流程支持
    @Override
    public CompletableFuture<List<DiscoveredItem>> discover(DiscoveryQuery query) {
        return CompletableFuture.supplyAsync(() -> {
            String endpoint = buildEndpoint(query);
            String response = httpClient.get(endpoint);
            return parseResponse(response, query.getType());
        });
    }
    
    // API 端点设计
    // GET /scenes - 获取场景列表
    // GET /scenes/{sceneId} - 获取场景详情
    // GET /caps - 获取能力列表
    // GET /caps/{capId} - 获取能力详情
    // GET /skills - 获取技能列表
    // GET /skills/{skillId} - 获取技能详情
    // GET /skills?capId={capId} - 按能力查询技能
    
    // WebSocket 实时推送
    public void subscribeUpdates(Consumer<DiscoveryEvent> listener) {
        webSocketClient.connect(WS_URL, new WebSocketListener() {
            @Override
            public void onMessage(String message) {
                DiscoveryEvent event = parseEvent(message);
                listener.accept(event);
            }
        });
    }
}
```

#### 3.4.2 发现流程支持

| 流程阶段 | 支持方式 | 实现要点 |
|----------|----------|----------|
| 启动雷达扫描 | API 查询 | 调用 RESTful API 获取索引数据 |
| 展示选择界面 | 分类查询 | 支持按分类、关键词搜索 |
| 用户选择场景 | 详情查询 | 调用详情 API 获取完整信息 |
| 自动安装场景 | 下载 API | 调用下载 API 获取技能包 |

#### 3.4.3 可视化指导

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  SkillCenter 发现可视化                                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  界面元素:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 搜索栏                                                                  │
│     ├── 搜索输入框                                                          │
│     ├── 分类筛选                                                            │
│     └── 排序选项                                                            │
│  2. 结果列表                                                                │
│     ├── 卡片式展示                                                          │
│     ├── 官方认证标识                                                        │
│     ├── 评分和下载量                                                        │
│     └── 更新时间                                                            │
│  3. 详情面板                                                                │
│     ├── 完整描述                                                            │
│     ├── 能力列表                                                            │
│     ├── 技能列表                                                            │
│     └── 安装按钮                                                            │
│                                                                             │
│  交互流程:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 输入搜索 → 调用 API 搜索                                                 │
│  2. 点击结果 → 展示详情                                                      │
│  3. 点击安装 → 调用下载 API                                                  │
│  4. 实时更新 → WebSocket 推送                                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 3.5 GitHub/Gitee 发现源

#### 3.5.1 协议实现

```java
public class GitHubDiscoveryProvider implements DiscoveryProvider {
    
    // API 端点
    private static final String GITHUB_API = "https://api.github.com";
    private static final String GITEE_API = "https://gitee.com/api/v5";
    
    // 发现流程支持
    @Override
    public CompletableFuture<List<DiscoveredItem>> discover(DiscoveryQuery query) {
        return CompletableFuture.supplyAsync(() -> {
            List<DiscoveredItem> results = new ArrayList<>();
            
            // 1. 搜索仓库
            String searchQuery = buildSearchQuery(query);
            List<Repository> repos = searchRepositories(searchQuery);
            
            // 2. 解析仓库信息
            for (Repository repo : repos) {
                DiscoveredItem item = parseRepository(repo, query.getType());
                if (item != null) {
                    results.add(item);
                }
            }
            
            return results;
        });
    }
    
    // 获取仓库内容
    public SceneDetail getSceneDetail(String sceneId, String repoUrl) {
        // 1. 克隆仓库
        // 2. 解析 scene.yaml
        // 3. 解析 cap.yaml
        // 4. 返回详情
    }
}
```

#### 3.5.2 发现流程支持

| 流程阶段 | 支持方式 | 实现要点 |
|----------|----------|----------|
| 启动雷达扫描 | 仓库搜索 | 使用 GitHub/Gitee API 搜索 ooder-scene 仓库 |
| 展示选择界面 | 仓库列表 | 展示搜索结果，包含 stars、forks 等信息 |
| 用户选择场景 | 仓库内容 | 克隆仓库，解析 scene.yaml、cap.yaml |
| 自动安装场景 | Git Clone | 克隆仓库，安装技能 |

#### 3.5.3 可视化指导

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  GitHub/Gitee 发现可视化                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  界面元素:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 搜索栏                                                                  │
│     ├── 搜索输入框                                                          │
│     ├── 平台选择（GitHub/Gitee）                                            │
│     └── 排序选项（stars/updated）                                           │
│  2. 仓库列表                                                                │
│     ├── 仓库名称和描述                                                      │
│     ├── ⭐ Stars 数量                                                       │
│     ├── 🍴 Forks 数量                                                       │
│     ├── 更新时间                                                            │
│     └── 作者信息                                                            │
│  3. 仓库详情                                                                │
│     ├── README 预览                                                         │
│     ├── 文件列表                                                            │
│     ├── 能力列表                                                            │
│     └── 安装按钮                                                            │
│                                                                             │
│  交互流程:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 输入搜索 → 调用 GitHub/Gitee API                                         │
│  2. 点击仓库 → 展示详情                                                      │
│  3. 点击安装 → Git Clone + 安装                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 3.6 Git Repository 发现源

#### 3.6.1 协议实现

```java
public class GitRepositoryDiscoveryProvider implements DiscoveryProvider {
    
    // 发现流程支持
    @Override
    public CompletableFuture<List<DiscoveredItem>> discover(DiscoveryQuery query) {
        return CompletableFuture.supplyAsync(() -> {
            List<DiscoveredItem> results = new ArrayList<>();
            
            // 1. 获取配置的 Git 仓库列表
            List<String> repos = config.getGitRepositories();
            
            // 2. 遍历仓库
            for (String repoUrl : repos) {
                List<DiscoveredItem> items = scanRepository(repoUrl, query);
                results.addAll(items);
            }
            
            return results;
        });
    }
    
    // 扫描仓库
    private List<DiscoveredItem> scanRepository(String repoUrl, DiscoveryQuery query) {
        // 1. 克隆仓库（浅克隆）
        // 2. 扫描目录结构
        // 3. 解析 scene.yaml、cap.yaml、skill.yaml
        // 4. 返回发现结果
    }
}
```

#### 3.6.2 发现流程支持

| 流程阶段 | 支持方式 | 实现要点 |
|----------|----------|----------|
| 启动雷达扫描 | 仓库扫描 | 扫描配置的 Git 仓库列表 |
| 展示选择界面 | 仓库内容 | 展示仓库中的场景、能力、技能 |
| 用户选择场景 | 仓库解析 | 解析仓库中的 YAML 文件 |
| 自动安装场景 | Git Clone | 克隆仓库，安装技能 |

#### 3.6.3 可视化指导

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Git Repository 发现可视化                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  界面元素:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 仓库配置面板                                                            │
│     ├── 仓库 URL 列表                                                       │
│     ├── 添加/删除仓库                                                       │
│     └── 同步状态                                                            │
│  2. 仓库内容列表                                                            │
│     ├── 场景列表                                                            │
│     ├── 能力列表                                                            │
│     └── 技能列表                                                            │
│  3. 详情面板                                                                │
│     ├── 基本信息                                                            │
│     ├── 文件列表                                                            │
│     └── 安装按钮                                                            │
│                                                                             │
│  交互流程:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 配置仓库 URL → 扫描仓库                                                  │
│  2. 展示内容 → 选择场景/能力/技能                                            │
│  3. 点击安装 → 克隆安装                                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 3.7 Local FS 发现源

#### 3.7.1 协议实现

```java
public class LocalFsDiscoveryProvider implements DiscoveryProvider {
    
    // 缓存目录结构
    // ~/.ooder/
    // ├── cache/
    // │   ├── scenes/
    // │   │   ├── scene-index.yaml
    // │   │   └── {sceneId}/
    // │   │       ├── scene.yaml
    // │   │       └── scene.md
    // │   ├── capabilities/
    // │   │   ├── cap-index.yaml
    // │   │   └── {capId}/
    // │   │       ├── cap.yaml
    // │   │       └── cap.md
    // │   └── skills/
    // │       ├── skill-index.yaml
    // │       └── {skillId}/
    // │           ├── skill.yaml
    // │           └── skill.jar
    // └── installed/
    //     └── {sceneId}/
    //         ├── config.yaml
    //         └── skills/
    
    // 发现流程支持
    @Override
    public CompletableFuture<List<DiscoveredItem>> discover(DiscoveryQuery query) {
        return CompletableFuture.supplyAsync(() -> {
            List<DiscoveredItem> results = new ArrayList<>();
            
            // 1. 读取索引文件
            String indexPath = getIndexPath(query.getType());
            YamlIndex index = readIndex(indexPath);
            
            // 2. 遍历索引
            for (IndexEntry entry : index.getEntries()) {
                DiscoveredItem item = parseEntry(entry, query);
                results.add(item);
            }
            
            return results;
        });
    }
}
```

#### 3.7.2 发现流程支持

| 流程阶段 | 支持方式 | 实现要点 |
|----------|----------|----------|
| 启动雷达扫描 | 读取索引 | 读取 scene-index.yaml、cap-index.yaml、skill-index.yaml |
| 展示选择界面 | 解析索引 | 解析索引文件，展示分类列表 |
| 用户选择场景 | 读取详情 | 读取 scene.yaml、cap.yaml 文件 |
| 自动安装场景 | 本地安装 | 从缓存目录安装技能 |

#### 3.7.3 可视化指导

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Local FS 发现可视化                                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  界面元素:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 缓存状态面板                                                            │
│     ├── 缓存大小                                                            │
│     ├── 最后同步时间                                                        │
│     └── 清理缓存按钮                                                        │
│  2. 已安装列表                                                              │
│     ├── 已安装场景                                                          │
│     ├── 已安装技能                                                          │
│     └── 安装时间                                                            │
│  3. 离线模式指示                                                            │
│     ├── 在线/离线状态                                                       │
│     └── 离线可用能力                                                        │
│                                                                             │
│  交互流程:                                                                  │
│  ─────────────────────────────────────────────────────────────              │
│  1. 展示缓存内容 → 选择场景/能力/技能                                        │
│  2. 离线模式 → 仅显示缓存内容                                                │
│  3. 点击安装 → 从缓存安装                                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 四、上层可视化发现流程指导

### 4.1 统一发现界面设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    统一发现界面设计                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 顶部导航栏                                                            │   │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                     │   │
│  │ │ 个人网络 │ │ 部门分享 │ │ 公司管理 │ │ 公共社区 │                     │   │
│  │ └─────────┘ └─────────┘ └─────────┘ └─────────┘                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 搜索栏                                                                │   │
│  │ ┌───────────────────────────────────────────────────────────────┐   │   │
│  │ │ 🔍 搜索场景、能力、技能...                                      │   │   │
│  │ └───────────────────────────────────────────────────────────────┘   │   │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                     │   │
│  │ │ 全部    │ │ 场景    │ │ 能力    │ │ 技能    │                     │   │
│  │ └─────────┘ └─────────┘ └─────────┘ └─────────┘                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 发现状态指示                                                          │   │
│  │ ┌────────────────────────────────────────────────────────────────┐  │   │
│  │ │ 🟢 UDP (3 设备)  🟢 mDNS (2 服务)  🟢 SkillCenter  🟢 缓存     │  │   │
│  │ └────────────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 结果展示区                                                            │   │
│  │ ┌─────────────────────────────────────────────────────────────────┐ │   │
│  │ │ 卡片式展示                                                        │ │   │
│  │ │ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                 │ │   │
│  │ │ │ 场景卡片    │ │ 能力卡片    │ │ 技能卡片    │                 │ │   │
│  │ │ │ - 名称      │ │ - 名称      │ │ - 名称      │                 │ │   │
│  │ │ │ - 描述      │ │ - 版本      │ │ - 版本      │                 │ │   │
│  │ │ │ - 来源      │ │ - 来源      │ │ - 来源      │                 │ │   │
│  │ │ │ - 安装按钮  │ │ - 详情按钮  │ │ - 安装按钮  │                 │ │   │
│  │ │ └─────────────┘ └─────────────┘ └─────────────┘                 │ │   │
│  │ └─────────────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 发现流程交互设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    发现流程交互设计                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  步骤1: 选择发现范围                                                         │
│  ─────────────────────────────────────────────────────────────              │
│  用户操作: 点击顶部导航栏选择范围                                            │
│  系统响应:                                                                   │
│  ├── 切换发现源组合                                                         │
│  ├── 更新发现状态指示                                                       │
│  └── 刷新结果列表                                                           │
│                                                                             │
│  步骤2: 输入搜索关键词                                                       │
│  ─────────────────────────────────────────────────────────────              │
│  用户操作: 在搜索栏输入关键词                                                │
│  系统响应:                                                                   │
│  ├── 并行查询多个发现源                                                     │
│  ├── 显示查询进度                                                           │
│  ├── 聚合查询结果                                                           │
│  └── 更新结果列表                                                           │
│                                                                             │
│  步骤3: 浏览结果                                                             │
│  ─────────────────────────────────────────────────────────────              │
│  用户操作: 浏览结果卡片                                                      │
│  系统响应:                                                                   │
│  ├── 显示基本信息                                                           │
│  ├── 显示来源标识                                                           │
│  └── 显示可信度标识                                                         │
│                                                                             │
│  步骤4: 查看详情                                                             │
│  ─────────────────────────────────────────────────────────────              │
│  用户操作: 点击卡片查看详情                                                  │
│  系统响应:                                                                   │
│  ├── 从发现源获取详细信息                                                   │
│  ├── 展示完整描述                                                           │
│  ├── 展示能力列表（场景）                                                   │
│  ├── 展示技能列表（能力）                                                   │
│  └── 展示对比信息                                                           │
│                                                                             │
│  步骤5: 安装场景/技能                                                        │
│  ─────────────────────────────────────────────────────────────              │
│  用户操作: 点击安装按钮                                                      │
│  系统响应:                                                                   │
│  ├── 显示安装进度                                                           │
│  ├── 下载技能包                                                             │
│  ├── 验证 CAP 契约                                                          │
│  ├── 安装依赖技能                                                           │
│  ├── 初始化场景 Agent                                                       │
│  ├── 激活场景                                                               │
│  └── 缓存到本地                                                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.3 发现状态可视化

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    发现状态可视化                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  发现源状态指示:                                                            │
│  ─────────────────────────────────────────────────────────────              │
│  🟢 在线且可用                                                              │
│  🟡 在线但响应慢                                                            │
│  🔴 离线或不可用                                                            │
│  🔵 正在连接                                                                │
│  ⚫ 禁用                                                                    │
│                                                                             │
│  发现进度指示:                                                              │
│  ─────────────────────────────────────────────────────────────              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 正在扫描...                                                           │   │
│  │ ████████████░░░░░░░░░░░░░░░░░░░░ 40%                                 │   │
│  │                                                                       │   │
│  │ UDP Broadcast: ✅ 完成 (3 设备)                                       │   │
│  │ mDNS: ✅ 完成 (2 服务)                                                │   │
│  │ SkillCenter: 🔄 查询中...                                             │   │
│  │ Local FS: ✅ 完成 (10 缓存)                                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  结果来源标识:                                                              │
│  ─────────────────────────────────────────────────────────────              │
│  🌐 来自 SkillCenter (官方)                                                 │
│  📡 来自 UDP Broadcast (局域网)                                             │
│  💻 来自 Local FS (本地缓存)                                                │
│  🐙 来自 GitHub (社区)                                                      │
│  🏢 来自 Git Repository (企业)                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 五、实现优先级建议

### 5.1 第一阶段（核心功能）

1. **Local FS 发现源** - 离线支持的基础
2. **SkillCenter API 发现源** - 官方能力的主要来源
3. **UDP Broadcast 发现源** - 个人网络发现

### 5.2 第二阶段（扩展功能）

1. **mDNS 发现源** - 增强个人网络发现
2. **GitHub/Gitee 发现源** - 公共社区能力

### 5.3 第三阶段（高级功能）

1. **DHT 发现源** - 分布式发现
2. **Git Repository 发现源** - 企业内部发现

---

## 六、总结

本文档通过严格推理分析，详细说明了各发现源如何支持发现流程，并给出了实现指导和上层可视化发现流程的指导。关键要点：

1. **统一抽象**：通过 DiscoveryProvider 接口统一抽象所有发现源
2. **分层设计**：根据发现范围选择合适的发现源组合
3. **优先级策略**：根据场景选择最优的发现源优先级
4. **可视化指导**：为上层界面设计提供详细的交互流程指导

通过本文档的指导，可以实现一个灵活、可扩展、用户友好的能力发现系统。
